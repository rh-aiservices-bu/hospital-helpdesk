= Just a Model

[role="_abstract"]
// Introduction to working with a model directly.

First thing we need for our Hospital Helpdesk assistant is a model. +
In this section we will deploy our own model (it's also common to use external ones, but we are all about on-prem), and then interact with that model without any additional bells and whistles.
